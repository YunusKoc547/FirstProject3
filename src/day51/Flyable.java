package day51;

public interface Flyable {

    public abstract void fly();
}

// An interface can extend another interface
// to inherit it's members for re-usability
// This is also an inheritance relationship
// in this example
// Flyable is super type of UprightFlyable
// so it inherit methods of Flyable


